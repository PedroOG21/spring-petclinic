database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://spring-petclinic-flex.postgres.database.azure.com/petclinic?sslmode=require}
spring.datasource.username=${POSTGRES_USER:pedroog21}
spring.datasource.password=${POSTGRES_PASS:Admin123}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always